VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Express API: leave unset in local dev to use Vite proxy (/api and /health → http://localhost:3000).
# Set explicitly for production, e.g. https://api.yourdomain.com  (no trailing slash)
# VITE_API_URL=http://localhost:3000
